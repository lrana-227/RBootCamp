// Array of names
var names = ["Jane", "John", "Jimbo", "Jedediah"];

// Slice the first two names
var first_two = names.slice(0,2);
console.log(first_two);
// Slice the last two names
var last_two = names.slice(2,4);
console.log(last_two);